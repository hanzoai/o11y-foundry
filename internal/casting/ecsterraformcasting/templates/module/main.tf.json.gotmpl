{{- $name := $.Metadata.Name -}}
{
  "resource": {
    "aws_service_discovery_private_dns_namespace": {
      "main": {
        "name": "{{ $name }}.local",
        "vpc": "${var.vpc_id}"
      }
    }
  }
}
