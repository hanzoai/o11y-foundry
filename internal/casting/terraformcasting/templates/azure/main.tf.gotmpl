# Azure Terraform configuration for {{ .Metadata.Name }}
# Generated by foundry - do not edit manually
#
# NOTE: Azure support is coming soon. This is a placeholder template.

locals {
  name = "{{ .Metadata.Name }}"
  
  common_tags = {
    "app.kubernetes.io/managed-by" = "foundry"
    "app.kubernetes.io/name"       = "{{ .Metadata.Name }}"
  }
}

# TODO: Implement Azure Container Instances or AKS resources for SigNoz components
